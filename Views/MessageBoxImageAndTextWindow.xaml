<Window x:Class="WordFlow.Views.MessageBoxImageAndTextWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WordFlow" Height="220" Width="450"
        WindowStartupLocation="CenterScreen"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 内容区 -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- 图标 -->
            <Image Grid.Column="0" Source="pack://application:,,,/Resources/icon.ico" 
                   Width="48" Height="48" Margin="0,0,15,0"/>
            
            <!-- 文本内容 -->
            <TextBlock Grid.Column="1" x:Name="MessageTextBlock" 
                       TextWrapping="Wrap" FontSize="14" 
                       VerticalAlignment="Center"/>
        </Grid>
        
        <!-- 按钮区 -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button x:Name="Button1" Content="确定" MinWidth="80" Padding="15,8" 
                    Margin="0,0,10,0" Click="Button_Click"/>
            <Button x:Name="Button2" Content="取消" MinWidth="80" Padding="15,8" 
                    Margin="0,0,10,0" Click="Button_Click" Visibility="Collapsed"/>
            <Button x:Name="Button3" Content="取消" MinWidth="80" Padding="15,8" 
                    Click="Button_Click" Visibility="Collapsed"/>
        </StackPanel>
    </Grid>
</Window>
