# WordFlow v1.0

WordFlow 는 로컬 ASR 음성 인식을 지원하는 지능형 음성 입력 도구입니다. 음성을 실시간으로 텍스트로 변환하여任意应用程序에 입력할 수 있습니다.

**v1.0 릴리스** - .NET 8 WPF 로 구축되어 안정적이고 지능적인 음성 입력 경험을 제공합니다.

---

## 📥 지금 다운로드

### 옵션 1: 설치 프로그램 (권장)

| 다운로드 | 크기 | 설명 |
|----------|------|-------------|
| [⬇️ WordFlow_Setup.exe](https://github.com/yanlin-cheng/WordFlow/releases/download/v1.0.0/WordFlow_Setup.exe) | 약 350 MB | **완전 설치 프로그램** (.NET 8 런타임 및 Python 환경 포함, 추가 설치 불필요) |

### 옵션 2: 수동 모델 다운로드 (선택사항)

자동 다운로드가 실패한 경우, 모델 파일을 `PythonASR/models/` 디렉토리에 수동으로 다운로드하세요:

| 다운로드 | 크기 | 설명 |
|----------|------|-------------|
| [🧠 SenseVoice 모델 (1/2)](https://github.com/yanlin-cheng/WordFlow/releases/download/v1.0.0/sensevoice-small-onnx.zip.001) | 90 MB | 음성 모델 파트 1 |
| [🧠 SenseVoice 모델 (2/2)](https://github.com/yanlin-cheng/WordFlow/releases/download/v1.0.0/sensevoice-small-onnx.zip.002) | 57 MB | 음성 모델 파트 2 |

> **💡 팁**: 설치 프로그램을 사용하는 것을 권장합니다. 모델은 첫 번째 실행 시 자동으로 다운로드됩니다.

---

## 🚀 빠른 시작

### 설치 단계

1. **설치 프로그램 다운로드**: `WordFlow_Setup.exe` 다운로드
2. **설치 프로그램 실행**: 더블클릭하고 지침에 따름
   - 설치 프로그램은 필요에 따라 .NET 8 런타임을 자동으로 감지하고 설치합니다
   - 설치 후 즉시 WordFlow 를 시작하는 옵션을 선택할 수 있습니다
3. **애플리케이션 시작**: 설치 후 WordFlow 사용 시작

### 모델 설치

**권장**: 애플리케이션은 첫 번째 실행 시 모델 다운로드를 프롬프트합니다

**대안**: 자동 다운로드가 실패한 경우:
1. WordFlow 시작
2. "모델 관리자" 버튼 클릭
3. 원하는 모델의 "다운로드" 버튼 클릭
4. 다운로드 완료 후 모델이 자동으로 로드됩니다

### 사용 방법

1. **WordFlow 시작** - 애플리케이션은 시스템 트레이로 최소화됩니다
2. **눌러서 말하기** - 음성 입력 단축키 (기본값: `CapsLock`) 를 눌러 녹음 시작
3. **놓아서 입력** - 단축키를 놓아 인식하고 대상 애플리케이션에 텍스트 입력

---

## ✨ 기능

### 핵심 기능

| 기능 | 설명 |
|---------|-------------|
| 🎤 **지능형 음성 입력** | 눌러서 말하기, 놓아서 인식 및 입력 |
| 🧠 **로컬 ASR 인식** | Sherpa-ONNX 프레임워크 사용, 오프라인 작동 |
| 📚 **개인 어휘** | AI 기반 수정 기능과 함께 사용자 정의 어휘 지원 |
| 📝 **기록 로그** | 입력 기록을 자동으로 저장, 쉽게 참조 가능 |
| 🔔 **트레이 통합** | 시스템 트레이로 최소화, 항상 접근 가능 |
| ⚡ **자동 시작** | 부팅 시 자동 시작 지원 |
| ⌨️ **글로벌 단축키** | 사용자 정의 가능한 단축키 |
| 🔄 **자동 모델 로드** | 다운로드 후 모델 자동 로드 |

### 단축키 설정

WordFlow 는 다양한 사용 습관에 맞는 단축키 구성을 지원합니다:

| 단축키 유형 | 기본값 | 설명 |
|-------------|---------|-------------|
| 음성 입력 단축키 | CapsLock | 눌러서 말하기, 놓아서 인식 |

> **💡 팁**: 음성 입력 단축키로 CapsLock 사용을 권장합니다:
> - 중앙에 위치하여 양손으로 쉽게 조작 가능
> - 키 표면이 크고 실수 접촉이 적음
> - 눌렀을 때 명확한 물리적 피드백

### 모델 관리자

WordFlow 는 모델 관리 기능을 제공합니다:
- **원클릭 다운로드**: "다운로드"를 클릭하여 모델 직접 다운로드
- **자동 로드**: 다운로드 후 모델 로드 자동 시도
- **서비스 감지**: ASR 서비스가 실행되지 않은 경우 시작 프롬프트
- **상태 표시**: 실시간 모델 상태 (설치됨/미다운로드/로드됨)

---

## 💻 시스템 요구사항

| 요구사항 | 사양 |
|-------------|---------------|
| 운영 체제 | Windows 10/11 64 비트 |
| 런타임 환경 | .NET 8.0 런타임 (설치 프로그램에 포함) |
| 디스크 공간 | 최소 500MB 여유 공간 (모델 파일 포함) |
| 네트워크 | 초기 모델 다운로드에 인터넷 연결 필요 |
| 마이크 | 사용 가능한 오디오 입력 장치 필요 |
| 메모리 | 4GB RAM 권장 |

---

## 🏗️ 기술 아키텍처

### 기술 스택

| 구성 요소 | 기술 | 설명 |
|-----------|------------|-------------|
| UI 프레임워크 | .NET 8 WPF | 성숙하고 안정적, 네이티브 Windows 지원 |
| 데이터베이스 | SQLite | 경량, 추가 서비스 불필요 |
| ORM | Dapper | 경량 ORM, 좋은 성능 |
| 음성 인식 | Sherpa-ONNX | Next-Gen K2 음성 인식 도구 키트 |
| 오디오 처리 | NAudio | 성숙한 .NET 오디오 라이브러리 |
| 분할 엔진 | Jieba.NET | 중국어 단어 분할 |
| 병음 변환 | TinyPinyin | 중국어 문자를 병음으로 변환 |
| 로깅 | 내장 Logger | 구조화된 로깅 |

### 디렉토리 구조

```
WordFlow/
├── WordFlow.exe              # 메인 애플리케이션
├── Data/
│   └── models.json           # 모델 구성
├── PythonASR/
│   ├── asr_server.py         # ASR 서버
│   ├── start_server.bat      # 시작 스크립트
│   └── models/
│       └── sensevoice-small-onnx/  # 음성 모델 (첫 번째 실행 시 다운로드)
│           ├── model.int8.onnx
│           └── tokens.txt
├── Resources/
│   └── icon.ico              # 애플리케이션 아이콘
└── Logs/                     # 로그 파일
```

### 서비스 아키텍처

```
┌─────────────────────────────────────────────────────────┐
│                    WordFlow 클라이언트                    │
│  ┌─────────────────────────────────────────────────┐   │
│  │              .NET WPF UI 레이어                   │   │
│  │  - 메인 창 (MainWindow)                         │   │
│  │  - 설정 창 (SettingsWindow)                     │   │
│  │  - 어휘 관리자 (VocabularyManagerWindow)        │   │
│  │  - 모델 관리자 (ModelManagerWindow)             │   │
│  │  - 첫 번째 실행 마법사 (FirstRunWizard)         │   │
│  └─────────────────────────────────────────────────┘   │
│                          ↓                               │
│  ┌─────────────────────────────────────────────────┐   │
│  │              .NET 서비스 레이어                   │   │
│  │  - SettingsService (설정 관리)                  │   │
│  │  - HistoryService (기록 로그/SQLite)            │   │
│  │  - VocabularyService (어휘 관리)                │   │
│  │  - VocabularyLearningEngine (어휘 학습)         │   │
│  │  - GlobalHotkeyService (글로벌 단축키)          │   │
│  │  - TrayService (시스템 트레이)                  │   │
│  │  - ModelDownloadService (모델 다운로드)         │   │
│  │  - SpeechRecognitionService (ASR 클라이언트)    │   │
│  └─────────────────────────────────────────────────┘   │
│                          ↓ HTTP                        │
│  ┌─────────────────────────────────────────────────┐   │
│  │          Python ASR 서비스 (별도 프로세스)        │   │
│  │  - Sherpa-ONNX 음성 인식                        │   │
│  │  - 모델 로드/전환                               │   │
│  │  - 오디오 전처리                                │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

---

## ❓ 자주 묻는 질문

### Q1: 첫 번째 실행 시 무엇을 다운로드해야 합니까?

첫 번째 실행 시 애플리케이션은 자동으로 음성 모델 파일 (약 156MB) 을 다운로드합니다. 안정적인 인터넷 연결을 확보하세요. 다운로드 완료 후 다시 다운로드하지 않고도 정상적으로 애플리케이션을 사용할 수 있습니다.

### Q2: 모델 다운로드가 실패하면 어떻게 해야 합니까?

자동 다운로드가 실패한 경우:
1. 네트워크 연결이 작동하는지 확인
2. 애플리케이션 재시도
3. 모델 파일 수동 다운로드 (위 "옵션 2" 참조)
4. 모델 관리자 페이지에서 "새로고침" 버튼 클릭

### Q3: 모델을 다운로드한 후 왜 사용할 수 없습니까?

WordFlow 는 이 문제를 최적화했습니다:
- 다운로드 후 모델 로드를 자동으로 시도
- ASR 서비스가 실행되지 않은 경우 시작 프롬프트
 그래도 작동하지 않으면 애플리케이션을 재시작하거나 "서비스 연결" 버튼을 수동으로 클릭

### Q4: 인식이 부정확하면 어떻게 해야 합니까?

1. 마이크가 정상적으로 작동하는지 확인
2. 조용한 환경에서 사용
3. 명확하고 적절한 속도로 말하기
4. 개인 어휘에 자주 사용하는 어휘 추가

### Q5: 일부 애플리케이션에서 텍스트를 입력할 수 없는 이유는 무엇입니까?

일부 애플리케이션은 외부 입력을 제한할 수 있습니다. 다음을 시도하세요:
1. WordFlow 를 관리자로 실행
2. 대상 애플리케이션에서 텍스트 상자를 먼저 클릭하여 포커스 획득
3. 보안 소프트웨어가 차단하지 않는지 확인

### Q6: 단축키를 변경하려면 어떻게 해야 합니까?

1. 시스템 트레이 아이콘 오른쪽 클릭
2. "설정" 선택
3. "단축키 설정" 섹션에서 수정

### Q7: 부팅 시 자동 시작이 안 되는 이유는 무엇입니까?

1. "부팅 시 자동 시작" 옵션이 활성화되어 있는지 확인
2. 보안 소프트웨어가 자동 시작을 차단하지 않는지 확인
3. 작업 관리자의 "시작 프로그램" 탭에서 확인

---

## 📋 변경 로그

### v1.0.0 (2026-03-15)

**초기 릴리스**
- 🎉 기본 음성 입력 기능 구현
- 🎉 개인 어휘 관리 구현
- 🎉 기록 로그 기능 구현
- 🎉 시스템 트레이 통합 구현
- 🎉 부팅 시 자동 시작 구현
- 🎉 글로벌 단축키 구현

**기술 기능**
- 🏗️ .NET 8 WPF 로 구축
- 🏗️ 모듈식 서비스 아키텍처
- 🏗️ Python 환경 및 ASR 서비스 내장
- 🏗️ 다중 모델 다운로드 및 관리 지원

**배포 최적화**
- 🚀 자체 포함 배포, .NET 런타임 설치 불필요
- 🚀 설치 프로그램 크기 약 350MB, 모든 종속성 포함
- 🚀 모델 다운로드를 첫 번째 실행 시로 이동, 다시 시작 지원
- 🚀 설치 프로세스 간소화, 성공률 향상

**알려진 문제**
- 일부 애플리케이션은 음성 입력 화면 기능을 완전히 지원하지 않을 수 있음

---

## 🔗 프로젝트 링크

| 플랫폼 | 링크 |
|----------|------|
| 📂 **GitHub 저장소** | https://github.com/yanlin-cheng/WordFlow |
| 🐛 **이슈 트래커** | https://github.com/yanlin-cheng/WordFlow/issues |

### 다국어 버전

- [🇨🇳 中文](README.md)
- [🇬🇧 English](README.en.md)
- [🇯🇵 日本語](README.ja.md)
- [🇰🇷 한국어](README.ko.md)

---

## 📄 라이선스

이 프로젝트는 **MIT + 전용** 이중 모드 라이선스를 사용합니다:

- **기본 기능** (MIT 라이선스): 핵심 음성 입력, 오프라인 모델, 로컬 어휘 관리 등
- **프리미엄 기능** (전용 라이선스): 사용자 로그인, 클라우드 동기화, 어휘 마켓플레이스, AI 향상 서비스 등

자세한 내용은 [LICENSE.txt](LICENSE.txt) 를 참조하세요.

---

## 🗺️ 로드맵

WordFlow 의 향후 개발 계획:

- **1 단계** (2026 년 3 월): 기반 개선, 오픈 소스 릴리스
- **2 단계** (2026 년 4-5 월): 사용자 시스템, 결제 통합
- **3 단계** (2026 년 6-7 월): 어휘 마켓플레이스 론칭
- **4 단계** (2026 년 8-9 월): AI 향상 서비스
- **5 단계** (2026 년 10 월 이후): 지속적 최적화, 다중 플랫폼 지원

자세한 내용은 [로드맵 문서](产品路线/WordFlow 产品路线与商业化规划.md) 를 참조하세요.

---

**WordFlow v1.0** - 음성 입력을 더 스마트하고 쉽게
